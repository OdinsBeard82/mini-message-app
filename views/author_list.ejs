<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title %>
    </title>
</head>

<body>
    <h1>
        <%= title %>
    </h1>
    <ul>
        <% author_list.forEach(function(author) { %>
            <li>
                <a href="<%= author.url %>">
                    <%= author.name %>
                </a>
            </li>
            <% }); %>
    </ul>
</body>

</html>